# 主日直播「进入直播」房间 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在主日直播页加「進入直播」按钮，点击进入全屏直播房间（左侧 YouTube 大画面 + 右侧参会人名单与聊天），复用现有身份/名单/聊天，零后端改动。

**Architecture:** `LiveStreamSection` 内新增 `roomOpen` state，渲染新组件 `LiveRoomView`（`fixed inset-0 z-50` 深色全屏覆盖层，风格仿 `MeetingRoomView`）。按钮状态与自动退房的判定抽成纯函数放 `Church/live/liveRoom.ts`，用 vitest（node 环境，无 jsdom）做 TDD。

**Tech Stack:** React 19 + TypeScript + Tailwind（Church/ 前端），vitest 3（`npm test`，配置只跑 `live/**`、`components/**` 等目录下的 `*.test.ts`）。

**Spec:** `docs/superpowers/specs/2026-07-08-live-room-design.md`

**约定:** 所有命令在 `Church/` 目录下执行。commit 一律不加 Co-Authored-By 尾注（用户要求只以 Gary 身份提交）。译文 zh 用繁体（与 translations.ts 现有条目一致）。

---

## File Structure

| 文件 | 动作 | 职责 |
| --- | --- | --- |
| `Church/live/liveRoom.ts` | Create | 纯逻辑：按钮三态（hidden/disabled/enabled）、自动退房判定 |
| `Church/live/liveRoom.test.ts` | Create | 上述逻辑的单元测试 |
| `Church/constants/translations.ts` | Modify | `liveChat` 块新增 3 个 key |
| `Church/components/LiveRoomView.tsx` | Create | 全屏直播房间覆盖层（顶栏 + YouTube 主区 + 右侧名单/聊天） |
| `Church/components/LiveStreamSection.tsx` | Modify | 两处按钮、roomOpen/pendingRoomEntry state、自动退房 effect、渲染覆盖层 |

---

### Task 1: 纯逻辑 `liveRoom.ts`（TDD）

**Files:**
- Create: `Church/live/liveRoom.ts`
- Test: `Church/live/liveRoom.test.ts`

- [ ] **Step 1: 写失败的测试**

创建 `Church/live/liveRoom.test.ts`：

```ts
import { describe, expect, it } from 'vitest';
import { liveRoomButtonState, shouldCloseLiveRoom } from './liveRoom';

describe('liveRoomButtonState', () => {
  it('直播中且有 videoId → enabled', () => {
    expect(liveRoomButtonState('live', 'abc123')).toBe('enabled');
  });
  it('直播中但 videoId 缺失 → disabled（暂时没画面可进）', () => {
    expect(liveRoomButtonState('live', null)).toBe('disabled');
  });
  it('离线 → disabled（置灰显示）', () => {
    expect(liveRoomButtonState('offline', null)).toBe('disabled');
  });
  it('回放 → hidden（不显示按钮）', () => {
    expect(liveRoomButtonState('replay', 'abc123')).toBe('hidden');
  });
});

describe('shouldCloseLiveRoom', () => {
  it('房间开着、直播结束（replay）→ 关', () => {
    expect(shouldCloseLiveRoom(true, 'replay', 'abc123')).toBe(true);
  });
  it('房间开着、切到 offline → 关', () => {
    expect(shouldCloseLiveRoom(true, 'offline', null)).toBe(true);
  });
  it('房间开着、直播中途丢失 videoId → 关', () => {
    expect(shouldCloseLiveRoom(true, 'live', null)).toBe(true);
  });
  it('房间开着、仍在直播 → 不关', () => {
    expect(shouldCloseLiveRoom(true, 'live', 'abc123')).toBe(false);
  });
  it('房间没开 → 永远不需要关', () => {
    expect(shouldCloseLiveRoom(false, 'offline', null)).toBe(false);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npm test -- live/liveRoom.test.ts`（在 `Church/` 下）
Expected: FAIL — `Cannot find module './liveRoom'`（或等价的模块不存在错误）

- [ ] **Step 3: 最小实现**

创建 `Church/live/liveRoom.ts`：

```ts
import type { LiveStreamPublicState } from '../types';

export type LiveRoomButtonState = 'hidden' | 'disabled' | 'enabled';

type StreamStatus = LiveStreamPublicState['status'];

/**
 * 「進入直播」按鈕三態：
 *   live + videoId → enabled（紅色可點）
 *   live 但缺 videoId / offline → disabled（置灰）
 *   replay → hidden（沒有直播可進）
 */
export function liveRoomButtonState(status: StreamStatus, videoId: string | null): LiveRoomButtonState {
  if (status === 'replay') return 'hidden';
  if (status === 'live' && videoId) return 'enabled';
  return 'disabled';
}

/** 直播結束或丟失 videoId 時，開着的房間應自動關閉退回直播頁。 */
export function shouldCloseLiveRoom(roomOpen: boolean, status: StreamStatus, videoId: string | null): boolean {
  return roomOpen && !(status === 'live' && !!videoId);
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npm test -- live/liveRoom.test.ts`
Expected: PASS，9 个用例全绿

- [ ] **Step 5: Commit**

```bash
git add Church/live/liveRoom.ts Church/live/liveRoom.test.ts
git commit -m "feat(live): button state + auto-close logic for live room"
```

---

### Task 2: 翻译 key

**Files:**
- Modify: `Church/constants/translations.ts`（`liveChat` 块，`refreshHint` 之后、`tapToResumeAudio` 之前）

- [ ] **Step 1: 添加 3 个 key**

在 `Church/constants/translations.ts` 中找到：

```ts
    refreshHint: { en: 'If you know the stream just started but the page hasn\'t updated, click to check now.', zh: '如知道直播剛開始但頁面尚未顯示，請點此立即檢查。' },
    tapToResumeAudio: { en: 'Tap to resume audio', zh: '點此恢復聲音' },
```

改为：

```ts
    refreshHint: { en: 'If you know the stream just started but the page hasn\'t updated, click to check now.', zh: '如知道直播剛開始但頁面尚未顯示，請點此立即檢查。' },
    enterRoom: { en: 'Enter Live Room', zh: '進入直播' },
    enterRoomOffline: { en: 'Available when the stream is live', zh: '直播開始後可進入' },
    leaveRoom: { en: 'Back', zh: '返回' },
    tapToResumeAudio: { en: 'Tap to resume audio', zh: '點此恢復聲音' },
```

- [ ] **Step 2: 类型检查**

Run: `npx tsc --noEmit`（在 `Church/` 下）
Expected: 无错误（与改动前基线一致）

- [ ] **Step 3: Commit**

```bash
git add Church/constants/translations.ts
git commit -m "feat(live): i18n keys for enter-live-room button"
```

---

### Task 3: `LiveRoomView` 组件

**Files:**
- Create: `Church/components/LiveRoomView.tsx`

依赖的现有组件接口（原样复用，不改动）：
- `LivePlayer`: props `{ videoId: string }`，渲染 YouTube iframe 铺满容器
- `LiveViewerList`: props `{ viewers: LiveStreamViewer[]; myDisplayName?: string }`（浅色样式）
- `LiveChatPanel`: props `{ videoId: string | null; sessionId: string; displayName: string; isAdmin?: boolean; enabled: boolean }`（浅色样式）
- `lucide-react` 已是依赖（MeetingRoomView 在用 `ChevronLeft`、`Users`）

- [ ] **Step 1: 创建组件**

创建 `Church/components/LiveRoomView.tsx`：

```tsx
import React, { useEffect } from 'react';
import { ChevronLeft, Users } from 'lucide-react';
import { useLocalization } from '../hooks/useLocalization';
import type { LiveStreamPublicState } from '../types';
import LivePlayer from './LivePlayer';
import LiveViewerList from './LiveViewerList';
import LiveChatPanel from './LiveChatPanel';

interface LiveRoomViewProps {
  state: LiveStreamPublicState;
  identity: { sessionId: string; displayName: string };
  isAdmin: boolean;
  /** 由父组件格式化好的开播时间文案（含前缀），空串则不显示。 */
  startedAtLabel: string;
  onLeave: () => void;
}

/**
 * 主日直播全屏房间：仿 MeetingRoomView 的深色壳（顶栏 + 主区 + 右栏），
 * 主区放 YouTube 直播，右栏复用直播页的名单 + 聊天（保持浅色，装在白底卡片里）。
 * 不接 LiveKit，纯观看。数据全部由 LiveStreamSection 传入，自身无请求逻辑。
 */
const LiveRoomView: React.FC<LiveRoomViewProps> = ({ state, identity, isAdmin, startedAtLabel, onLeave }) => {
  const { t } = useLocalization();

  // 房间打开期间锁定页面滚动，关闭时恢复
  useEffect(() => {
    const prev = document.body.style.overflow;
    document.body.style.overflow = 'hidden';
    return () => { document.body.style.overflow = prev; };
  }, []);

  return (
    <div className="fixed inset-0 z-50 flex flex-col bg-gray-950 text-gray-100">
      {/* 顶栏 */}
      <header className="flex shrink-0 items-center justify-between gap-3 border-b border-white/10 bg-gray-900/80 px-4 py-3">
        <button
          type="button"
          onClick={onLeave}
          className="flex min-w-0 items-center gap-1 text-sm font-semibold text-gray-300 hover:text-white"
        >
          <ChevronLeft size={18} className="shrink-0" />
          <span className="truncate">{t('liveChat.leaveRoom')}</span>
        </button>
        <div className="flex min-w-0 items-center gap-3">
          <span className="truncate font-bold">{t('sermonsPage.navLiveStream')}</span>
          <span className="flex shrink-0 items-center gap-1.5 text-xs font-semibold text-red-500">
            <span className="inline-block h-2 w-2 animate-pulse rounded-full bg-red-600" />
            {t('sermonsPage.liveBadge')}
          </span>
          {startedAtLabel && (
            <span className="hidden shrink-0 text-sm text-gray-400 sm:inline">{startedAtLabel}</span>
          )}
        </div>
        <div className="flex shrink-0 items-center gap-1.5 text-sm text-gray-400">
          <Users size={16} />
          {state.websiteTotal}
        </div>
      </header>

      {/* 主体：桌面左右分栏，移动端上下堆叠 */}
      <div className="flex min-h-0 flex-1 flex-col gap-3 p-3 lg:flex-row">
        <main className="min-h-0 min-w-0 flex-none lg:flex-1">
          <div className="aspect-video w-full overflow-hidden rounded-xl bg-black lg:aspect-auto lg:h-full">
            <LivePlayer videoId={state.videoId ?? ''} />
          </div>
        </main>

        {/* 右栏：名单(上, ~1/6) + 聊天(下, 其余)。组件保持浅色，白底卡片容器 */}
        <aside className="flex min-h-0 flex-1 flex-col overflow-hidden rounded-xl bg-white text-gray-900 shadow-lg lg:w-[320px] lg:flex-none">
          <div className="flex h-1/6 min-h-[72px] shrink-0 flex-col border-b border-gray-200 bg-gray-50/50">
            <div className="bg-gray-100 px-3 py-1.5 text-xs font-semibold uppercase tracking-wide text-gray-500">
              {t('liveChat.onlineHeader')}
            </div>
            <div className="min-h-0 flex-1 overflow-y-auto">
              <LiveViewerList viewers={state.viewerList} myDisplayName={identity.displayName} />
            </div>
          </div>
          <div className="flex min-h-0 flex-1 flex-col">
            <LiveChatPanel
              videoId={state.videoId}
              sessionId={identity.sessionId}
              displayName={identity.displayName}
              isAdmin={isAdmin}
              enabled
            />
          </div>
        </aside>
      </div>
    </div>
  );
};

export default LiveRoomView;
```

- [ ] **Step 2: 类型检查**

Run: `npx tsc --noEmit`（在 `Church/` 下）
Expected: 无新增错误

- [ ] **Step 3: Commit**

```bash
git add Church/components/LiveRoomView.tsx
git commit -m "feat(live): full-screen live room view (YouTube stage + viewers + chat)"
```

---

### Task 4: `LiveStreamSection` 接线

**Files:**
- Modify: `Church/components/LiveStreamSection.tsx`

改动点：imports、4 个 state/handler、自动退房 effect、LIVE 提示条按钮、覆盖层渲染、离线提示条置灰按钮。

- [ ] **Step 1: 加 imports**

在 `import LivePlayer from './LivePlayer';` 之后加：

```ts
import LiveRoomView from './LiveRoomView';
import { liveRoomButtonState, shouldCloseLiveRoom } from '../live/liveRoom';
```

- [ ] **Step 2: 加 state 与进房逻辑**

在 `const [selectedPastId, setSelectedPastId] = useState<string | null>(null);` 之后加：

```ts
const [roomOpen, setRoomOpen] = useState(false);
const [pendingRoomEntry, setPendingRoomEntry] = useState(false);
```

在 `handleJoin` 定义之前加进房 handler（兜底：没身份先弹 LiveJoinModal，入会后自动进房）：

```ts
const handleEnterRoom = useCallback(() => {
  if (!identity) {
    setPendingRoomEntry(true);
    setShowJoinModal(true);
    return;
  }
  setRoomOpen(true);
}, [identity]);
```

修改现有 `handleJoin`（在 `setShowJoinModal(false);` 之后、函数末尾加两行）：

```ts
const handleJoin = useCallback(async (params: { name?: string; asGuest?: boolean }) => {
  const sessionId = identity?.sessionId || newSessionId();
  const res = await api.liveJoin({ sessionId, name: params.name, asGuest: params.asGuest });
  const next: StoredIdentity = { sessionId, displayName: res.displayName, asGuest: !!params.asGuest };
  writeIdentity(next);
  setIdentity(next);
  setShowJoinModal(false);
  if (pendingRoomEntry) {
    setPendingRoomEntry(false);
    setRoomOpen(true);
  }
}, [identity, pendingRoomEntry]);
```

- [ ] **Step 3: 加自动退房 effect 与按钮状态**

在 `const countdown = useCountdown(...)` 之前加：

```ts
// 直播结束/丢失 videoId 时自动关房间，退回直播页（页面自然切到 replay/offline 布局）
useEffect(() => {
  if (shouldCloseLiveRoom(roomOpen, state?.status ?? 'offline', state?.videoId ?? null)) {
    setRoomOpen(false);
  }
}, [roomOpen, state?.status, state?.videoId]);

const roomButton = liveRoomButtonState(state?.status ?? 'offline', state?.videoId ?? null);
```

- [ ] **Step 4: LIVE 提示条加「進入直播」按钮 + 渲染覆盖层**

在 LIVE 分支中，红色提示条（`bg-red-50` 那个 div）内、`{state.startedAt && (...)}` 之后加右对齐按钮：

```tsx
{roomButton === 'enabled' && (
  <button
    type="button"
    onClick={handleEnterRoom}
    className="ml-auto rounded-md bg-red-600 px-3 py-1.5 text-xs font-bold text-white hover:bg-red-700"
  >
    {t('liveChat.enterRoom')}
  </button>
)}
```

同一 LIVE 分支的 JSX 顶部，`{showJoinModal && (...)}` 之后加覆盖层：

```tsx
{roomOpen && identity && (
  <LiveRoomView
    state={state}
    identity={identity}
    isAdmin={isLoggedInAdmin}
    startedAtLabel={state.startedAt ? `${t('sermonsPage.liveStartedAt')} ${formatStartedAt(state.startedAt, language)}` : ''}
    onLeave={() => setRoomOpen(false)}
  />
)}
```

- [ ] **Step 5: 离线提示条加置灰按钮**

离线分支（最后一个 return）里，刷新按钮 `<button ... onClick={handleRefresh} ...>` 之前加：

```tsx
{roomButton === 'disabled' && (
  <button
    type="button"
    disabled
    title={t('liveChat.enterRoomOffline')}
    className="flex h-10 flex-none cursor-not-allowed items-center rounded-md bg-gray-300 px-3 text-sm font-bold text-gray-500"
  >
    {t('liveChat.enterRoom')}
  </button>
)}
```

注意刷新按钮和新按钮的共同父容器是 `div.rounded-lg.border...flex.items-start.gap-3` —— 两个按钮会自然并排（新按钮在前）。

- [ ] **Step 6: 类型检查 + 全量测试**

Run: `npx tsc --noEmit` → Expected: 无新增错误
Run: `npm test` → Expected: 全部通过（含 Task 1 的 9 个新用例）

- [ ] **Step 7: Commit**

```bash
git add Church/components/LiveStreamSection.tsx
git commit -m "feat(live): enter-live-room button on notice bars + room overlay wiring"
```

---

### Task 5: 构建验证 + 手动核对

- [ ] **Step 1: 构建**

Run: `npm run build`（在 `Church/` 下）
Expected: vite build 成功，无类型/打包错误

- [ ] **Step 2: 手动验证（`npm run dev`）**

- 离线状态：「下次直播时间」提示条右侧，刷新按钮前有置灰的「進入直播」，hover 显示「直播開始後可進入」，不可点。
- 直播中（需真实/模拟 live 状态）：红色 LIVE 条右侧有红色「進入直播」；点击全屏进房；左侧 YouTube 播放，右侧名单+聊天与页面外同步；「返回」退出；直播结束后房间自动关闭。
- 移动端宽度：房间内上下堆叠（视频在上）。
- 房间打开时页面背景不可滚动，关闭后恢复。

- [ ] **Step 3: 最终 commit（如手动验证时有微调）**

```bash
git add -A Church/
git commit -m "fix(live): polish live room layout after manual verification"
```

（若无改动则跳过。）
